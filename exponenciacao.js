/**
 * 03) Crie uma função que recebe dois parâmetros, 
 * base e expoente, e retorne a base elevada ao expoente.
 */

function exponenciacao(num, expoente){
    console.log(`${num} ^ ${expoente} = ${Math.pow(num, expoente)}`)
}

exponenciacao(2, 10)